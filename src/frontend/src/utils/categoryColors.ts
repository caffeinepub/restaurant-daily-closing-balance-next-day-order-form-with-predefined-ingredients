/**
 * Category color utilities — colors removed as per new layout design.
 * Functions kept for API compatibility; all return empty string.
 */

export function getCategoryBgColor(_category: string): string {
  return "";
}

export function getCategoryInputBgColor(_category: string): string {
  return "";
}
